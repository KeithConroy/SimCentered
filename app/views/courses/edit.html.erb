<h2 class="page-header">Edit Course</h2>

<%= bootstrap_form_for @course, layout: :horizontal, control_col: "col-sm-9", url: course_path do |f| %>
  <%= f.text_field :title %>
  <%= f.select(:instructor_id, @faculty) %>
  <%= f.submit %>
<% end %>