<h2 class="page-header">Create a New Course</h2>

<%= bootstrap_form_for @course, layout: :horizontal, control_col: "col-sm-9", url: courses_path do |f| %>
  <%= f.text_field :title %>
  <%= f.select(:instructor_id, @faculty) %>
  <%= f.submit %>
<% end %>