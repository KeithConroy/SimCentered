<tr>
  <td><%= link_to full_name(@student), user_path(@student.id) %></td>
  <td class="icon">
    <a href="<%= @course.id %>/remove_student" data-student-id="<%= @student.id %>" class="remove-student">
      <i class="fa fa-minus"></i>
    </a>
  </td>
</tr>