
<h2 class="page-header">Rooms</h2>

<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse " id="bs-example-navbar-collapse-1">

      <form class="navbar-form navbar-left" role="search" accept-charset="UTF-8" action="/search" method="get">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search" id="s" name="q" type="text">
        </div>
        <button type="submit" class="btn btn-default" name="commit">Submit</button>
      </form>
      <ul class="nav navbar-nav navbar-right" data-toggle="tooltip" data-placement="top" title="New Room">
        <%= link_to new_organization_room_path(@organization, @room), class: "btn btn-primary navbar-btn" do %>
          <i class="glyphicon glyphicon-plus"></i>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div class="panel panel-default">
  <div class="table-responsive">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Room</th>
          <th>Number</th>
          <th>Building</th>
          <th>Edit</th>
          <th>Delete</th>
        </tr>
      </thead>
      <tbody>
        <% @rooms.each do |room| %>
        <tr>
          <td><%= link_to room.title, organization_room_path(room.organization_id, room.id) %></td>
          <td><%= room.number %></td>
          <td><%= room.building %></td>
          <td><%= link_to edit_organization_room_path(room.organization_id, room.id), class: "glyphicon" do %>
            <i class="fa fa-pencil-square-o fa-lg"></i>
          <% end %>
          </td>
          <td><%= link_to organization_room_path(room.organization_id, room.id), method: :delete, data: { confirm: 'Are you sure you want to delete this room?' }, class: "glyphicon" do %>
            <i class="fa fa-trash-o fa-lg"></i>
          <% end %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>